using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class JSONReader : MonoBehaviour
{
    public TextAsset jsonFile;

    void Start()
    {
        //Question fromJson = JsonUtility.FromJson<Question>(jsonFile.text);
        //Debug.Log(fromJson);
    }

}
